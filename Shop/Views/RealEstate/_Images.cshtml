@model Shop.Models.Realestate.ImageToDatabaseViewModel

<div class="form-group row col-sm-4 offset-2">
    <div>@Model.ImageTitle</div>
    <img class="card-img-top" scr="@Model.Image" asp-append-version="True"/>
</div>